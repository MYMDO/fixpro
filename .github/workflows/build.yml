name: Build FiXPro Firmware

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        run: pip3 install platformio

      - name: Build
        run: |
          cd firmware/platformio
          pio run

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fixpro-firmware
          path: firmware/platformio/.pio/build/pico/firmware.uf2

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download firmware
        uses: actions/download-artifact@v4
        with:
          name: fixpro-firmware
          path: ./

      - name: Get version
        id: version
        run: |
          VERSION="v2.0.$(date +%Y%m%d%H%M%S)"
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Rename firmware
        run: mv firmware.uf2 FiXPro.uf2

      - name: Create checksums
        run: sha256sum FiXPro.uf2 > SHA256SUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: FiXPro ${{ steps.version.outputs.VERSION }}
          files: FiXPro.uf2 SHA256SUMS.txt
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
